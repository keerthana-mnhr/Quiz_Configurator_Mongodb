<UserControl x:Class="Quiz_Configurator.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quiz_Configurator.Views"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Menu DockPanel.Dock="Top">
        <MenuItem Header="_File">
            <MenuItem Header="New Question Pack" Command="{Binding ConfigurationViewModel.CreateNewPackCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Plus" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Select Question Pack" Command="{Binding ConfigurationViewModel.SelectPackCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Folder" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Delete Question Pack" Command="{Binding ConfigurationViewModel.DeletePackCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Remove" />
                </MenuItem.Icon>
            </MenuItem>
            <Separator/>
            <Separator/>
            <MenuItem Header="Import Questions" InputGestureText="Ctrl+I">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Download" />
                </MenuItem.Icon>
            </MenuItem>
            <Separator/>
            <MenuItem Header="Exit" InputGestureText="Alt+F4" Click="Exit_Click">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="PowerOff" />
                </MenuItem.Icon>
            </MenuItem>
        </MenuItem>

        <MenuItem Header="_Edit">
            <MenuItem Header="Add Question" InputGestureText="Insert" Command="{Binding ConfigurationViewModel.AddQuestionFromMenuCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Plus"/>
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem Header="Delete Question" InputGestureText="Delete" Command="{Binding ConfigurationViewModel.DeleteQuestionCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Minus"/>
                </MenuItem.Icon>
            </MenuItem>

            <MenuItem Header="Pack Options" InputGestureText="Ctrl+O" Command="{Binding ConfigurationViewModel.EditPackCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="List"/>
                </MenuItem.Icon>
            </MenuItem>
        </MenuItem>

        <MenuItem Header="_View">
            <MenuItem Header="Play" InputGestureText="Ctrl+P" Command="{Binding PlayerViewModel.StartQuizCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Play" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Edit" InputGestureText="Ctrl+E">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Edit" />
                </MenuItem.Icon>
            </MenuItem>
        </MenuItem>
    </Menu>
</UserControl>